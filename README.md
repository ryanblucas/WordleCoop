# WordleCoop
WebRTC Wordle Clone; written in TypeScript and drawn with the CanvasRenderingContext2D API.
It also has a Node.JS console implementation; but since Node.JS doesn't have WebRTC support, there is no co-op.